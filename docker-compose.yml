services:
  localstack:
    # pinned: the `latest`/2026.x images require a license token since 2026;
    # 4.14.0 is the newest community edition (matches auth-service/ai-act-platform)
    image: localstack/localstack:4.14.0
    environment:
      SERVICES: ssm,dynamodb
      DEFAULT_REGION: eu-central-1
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:4566/_localstack/health | grep -q '\"dynamodb\": \"available\"'"]
      interval: 5s
      timeout: 5s
      retries: 24

  app:
    build: .
    # The runtime image intentionally excludes dev dependencies (aws-lambda-powertools
    # is a Lambda layer in production); sync the full lock here so the app boots
    # and the seed script can talk to LocalStack.
    command: >
      sh -c "uv sync --frozen &&
             uv run uvicorn app.api_handler:app --host 0.0.0.0 --port 8080"
    environment:
      APP_NAME: user-service
      STAGE: local
      DEFAULT_TIMEZONE: UTC
      AWS_ACCESS_KEY_ID: test
      AWS_SECRET_ACCESS_KEY: test
      AWS_DEFAULT_REGION: eu-central-1
      AWS_ENDPOINT_URL: http://localstack:4566
      JWT_SECRET_SSM_PARAM_NAME: /local/secrets/jwt-secret
    depends_on:
      localstack:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 24
